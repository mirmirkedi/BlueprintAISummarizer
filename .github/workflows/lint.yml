name: Lint

on:
  push:
  pull_request:

jobs:
  metadata:
    name: Validate metadata
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate plugin descriptor JSON
        run: python -m json.tool BlueprintAISummarizer.uplugin > /dev/null
